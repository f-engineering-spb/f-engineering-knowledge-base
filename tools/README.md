# Tools

This folder stores technical tools required to maintain the knowledge base.

At the clean-core stage, the only active tool is
`validate_knowledge_base.py`, which checks required files, UTF-8, registry
paths, registry IDs, and statuses.

New production tools should not be added as loose scripts. They must be attached
to a business module or service workflow and include purpose, dependencies,
usage, and verification method.

